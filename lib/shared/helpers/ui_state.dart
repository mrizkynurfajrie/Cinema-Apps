enum UIState { success, loading, empty, error, initial }

enum MovieState { topRated, upcoming, nowPlaying, popular }

enum TvShowState { popular, topRated, onTheAir, airingToday }

enum PeopleState { popular }
